# Секреты — НИКОГДА не коммитить
# .env.* обязателен: правило «.env» ловит только точное имя, а рядом заводятся
# резервные копии вида .env.bak.20260717-012504 — в них лежит живой токен бота.
.env
.env.*
!.env.example
service-account.json
*.serviceaccount.json

node_modules
